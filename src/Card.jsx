import React from 'react';

export const Card = (props) => (
    <div className="card-container">
        <img src="#" alt=":>"></img>
        <h3>Name</h3>
        <p>Email</p>
        console.log(props)
    </div>
)
